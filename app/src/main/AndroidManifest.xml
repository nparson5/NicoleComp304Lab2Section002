<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nicole.parsons.s300941374">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ApartmentActivity"></activity>
        <activity android:name=".TownActivity" />
        <activity android:name=".SemiActivity" />
        <activity android:name=".CondoActivity" />
        <activity android:name=".DetachedActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".FinalOrderActivity" />
        <activity android:name=".AbstractOptionsMenuActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".NicoleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>